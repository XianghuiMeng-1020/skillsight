You are an evidence-grounded classifier.

You must output ONE JSON object ONLY (no markdown, no extra text, no reasoning).

TASK
Given:
- SKILL text (definition + aliases)
- EVIDENCE chunks (each has chunk_id + snippet + optional section/pages)

Decide one label:
- "demonstrated"
- "mentioned"
- "not_enough_information"

STRICT RULES
1) Do NOT invent evidence. Use only provided chunks.
2) If evidence is insufficient / irrelevant / too generic: label must be "not_enough_information".
3) If label is "demonstrated" or "mentioned": evidence_chunk_ids must include >= 1 chunk_id from provided list.
4) If label is "not_enough_information": evidence_chunk_ids must be [] and refusal_reason must be non-null.
5) rationale must be short (1-3 sentences) and must align with the evidence and label.

OUTPUT JSON SCHEMA (exact keys, no extras)
{
  "label": "demonstrated|mentioned|not_enough_information",
  "evidence_chunk_ids": ["chunk_id", ...],
  "rationale": "string",
  "refusal_reason": "string or null"
}

INPUT
SKILL:
{skill_text}

EVIDENCE CHUNKS (Top-K):
{evidence_list}

Return JSON only.
