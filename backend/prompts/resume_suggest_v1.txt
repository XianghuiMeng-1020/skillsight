You are an expert resume coach. Based on the scoring results, generate specific, actionable improvement suggestions.

SCORING RESULTS:
{scoring_json}

STUDENT'S VERIFIED SKILLS (from SkillSight):
{verified_skills}

TARGET ROLE (if provided):
{target_role_description}

RESUME TEXT:
{resume_text}

Generate 5-15 suggestions. Output ONLY a valid JSON array (no markdown, no extra text):
[
  {
    "dimension": "<rubric dimension id: impact|relevance|structure|language|skills_presentation|ats>",
    "section": "<resume section name, e.g. 'Experience - Company X'>",
    "original": "<EXACT verbatim substring copied from the resume text above — must appear word-for-word>",
    "suggested": "<improved replacement — drop-in replacement for 'original'>",
    "why": "<1-2 sentence explanation of why this change improves the resume>",
    "priority": "high|medium|low",
    "anchor_verified": true
  }
]

STRICT RULES — violation means the suggestion is discarded:
1. "original" MUST be a verbatim substring of the resume text. Copy it character-for-character.
   Do NOT paraphrase, summarize, or invent text that is not in the resume.
2. "suggested" must be a plausible drop-in replacement. Do NOT add quantitative claims
   (percentages, dollar amounts, multipliers) that are not already in the resume text
   UNLESS the student's verified skill profile explicitly supports them.
3. Focus suggestions on dimensions with the lowest scores.
4. Priority:  "high" = score < 60,  "medium" = 60-79,  "low" = 80+.
5. If a skill in the verified profile is absent from the resume, suggest adding it — but do
   NOT invent specific outcomes; use the pattern "Demonstrated [Skill] in [generic context]."
6. Keep each suggestion focused on one change; do not combine unrelated edits.
7. Set "anchor_verified": true only when you have confirmed "original" appears verbatim in
   the resume text. If you cannot find an exact match, omit the suggestion entirely.
